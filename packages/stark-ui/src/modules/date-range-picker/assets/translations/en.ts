import { type TranslationObject } from "@ngx-translate/core";

/**
 * @ignore
 */
export const translationsEn: TranslationObject = {
	STARK: {
		DATE_RANGE_PICKER: {
			FROM: "From",
			TO: "To"
		}
	}
};
