import { type TranslationObject } from "@ngx-translate/core";

/**
 * @ignore
 */
export const translationsNl: TranslationObject = {
	STARK: {
		DATE_RANGE_PICKER: {
			FROM: "Van",
			TO: "Tot"
		}
	}
};
